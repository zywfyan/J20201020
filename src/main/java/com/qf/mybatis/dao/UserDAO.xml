<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.mybatis.dao.UserDao">

    <select id="queryById" resultType="com.qf.mybatis.pojo.User">
        select * from tb_user where id = #{id}
    </select>

    <select id="queryAll" resultType="com.qf.mybatis.pojo.User">
        select * from tb_user
    </select>
</mapper>